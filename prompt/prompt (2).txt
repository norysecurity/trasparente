



Você é um ENGENHEIRO DE SOFTWARE SÊNIOR + ARQUITETO DE SISTEMAS DE IA INVESTIGATIVA.
Você está trabalhando em um sistema de auditoria governamental baseado em grafos (Neo4j), backend Python (FastAPI + Uvicorn) e frontend React/Next.js com renderização Markdown.

CONTEXTO ATUAL DO SISTEMA:
- O servidor backend sobe corretamente (Uvicorn rodando em http://0.0.0.0:8000).
- O frontend consegue chamar a API.
- Porém, ao tentar buscar detalhes de políticos específicos (IDs como 800004 e 800001), o backend falha devido a erros introduzidos na última refatoração.
- O sistema foi migrado para um modelo OFFLINE-FIRST, onde o agente de IA consulta o banco de grafos em vez de receber grandes estruturas via argumento.

OBJETIVO FINAL DO SISTEMA:
- Executar uma auditoria profunda (malha fina) em agentes públicos.
- Detectar divergências, anomalias e possíveis fraudes.
- Gerar um LAUDO INVESTIGATIVO DA IA.
- CADA divergência, alerta ou possível fraude DEVE conter UM LINK CLICÁVEL EM MARKDOWN apontando diretamente para a FONTE OFICIAL DO GOVERNO (Portal da Transparência, PNCP, Receita Federal, TSE etc).
- O frontend já renderiza Markdown via <ReactMarkdown>, portanto os links devem vir prontos do backend/IA.

────────────────────────────────────────
CORREÇÃO OBRIGATÓRIA — ERRO 1
────────────────────────────────────────

ERRO IDENTIFICADO NO TERMINAL:
NameError: name 'GoogleDriveManager' is not defined

CAUSA:
- O arquivo backend/agente_coletor_autonomo.py utiliza a classe GoogleDriveManager, porém ela NÃO FOI IMPORTADA.
- Além disso, esse agente também depende explicitamente da conexão com o banco Neo4j.

AÇÃO OBRIGATÓRIA:
No arquivo backend/agente_coletor_autonomo.py, você DEVE:

1. Localizar o topo do arquivo.
2. Logo abaixo de `import logging`, adicionar EXATAMENTE estes imports:

from motor_ia_qwen import AuditorGovernamentalIA
from google_drive_manager import GoogleDriveManager
from database.neo4j_conn import get_neo4j_connection

Esses imports são obrigatórios para que:
- o agente consiga acessar a IA
- o agente consiga acessar arquivos/documentos
- o agente consiga consultar o grafo local

────────────────────────────────────────
CORREÇÃO OBRIGATÓRIA — ERRO 2
────────────────────────────────────────

ERRO IDENTIFICADO NO TERMINAL:
Erro Worker: auditar_malha_fina_assincrona() takes 3 positional arguments but 7 were given

CAUSA:
- O código antigo chamava o worker passando 7 argumentos (CPF, red flags, despesas, etc).
- O NOVO MODELO OFFLINE-FIRST exige APENAS 3 argumentos:
  - id_politico
  - nome_politico
  - cpf
- Todo o restante DEVE ser buscado diretamente no banco de grafos Neo4j.

AÇÃO OBRIGATÓRIA NO backend/main.py:

1️⃣ CORRIGIR A FUNÇÃO DO WORKER

Localize aproximadamente a linha 125.
Substitua COMPLETAMENTE a função disparar_worker_assincrono por:

def disparar_worker_assincrono(id_politico: int, nome_politico: str, cpf: str):
    try:
        loop = asyncio.new_event_loop()
        asyncio.set_event_loop(loop)
        loop.run_until_complete(
            auditar_malha_fina_assincrona(id_politico, nome_politico, cpf)
        )
    except Exception as e:
        print(f"Erro Worker: {e}")
    finally:
        try:
            loop.close()
        except:
            pass

IMPORTANTE:
- NÃO enviar mais que 3 argumentos.
- Qualquer tentativa de enviar dados adicionais é ERRO DE ARQUITETURA.

2️⃣ CORRIGIR A CHAMADA DO BACKGROUND TASK

Localize aproximadamente a linha 265.
Substitua a chamada por:

background_tasks.add_task(
    disparar_worker_assincrono,
    id,
    nome_completo,
    cpf_oculto
)

────────────────────────────────────────
ATIVAÇÃO DOS LINKS CLICÁVEIS NA IA
────────────────────────────────────────

REQUISITO FUNCIONAL CRÍTICO:
O usuário exige que:
"cada divergência, possível fraude ou irregularidade exibida pela IA seja totalmente clicável e leve diretamente à fonte oficial do governo."

STATUS DO FRONTEND:
- O frontend JÁ SUPORTA ISSO.
- Existe uma seção chamada "Parecer do Fiscal IA".
- Existe um botão "Executar Varredura Profunda".
- O conteúdo é renderizado via Markdown.
- Portanto, NÃO É PROBLEMA DE FRONTEND.

O PROBLEMA ESTÁ NO CÉREBRO DA IA (PROMPT).

AÇÃO OBRIGATÓRIA NO backend/motor_ia_qwen.py (ou equivalente):

Você DEVE definir ou atualizar o SYSTEM PROMPT da IA com a seguinte REGRA ABSOLUTAMENTE ESTRITA:

────────────────────────────────────────
SYSTEM PROMPT OBRIGATÓRIO DA IA
────────────────────────────────────────

Você é um Auditor Investigativo Governamental.

REGRA INQUEBRÁVEL:
Ao descrever QUALQUER divergência, irregularidade, indício de corrupção, red flag ou anomalia:
- Você OBRIGATORIAMENTE DEVE apresentar a evidência com UM LINK EM MARKDOWN.
- O link DEVE apontar para a FONTE OFICIAL DO GOVERNO.
- Exemplos de fontes válidas:
  - Portal da Transparência
  - PNCP
  - Receita Federal
  - TSE
  - Tribunal de Contas
- NUNCA descreva uma fraude sem o link.

FORMATO OBRIGATÓRIO:
[Texto descritivo do documento ou evidência](URL_OFICIAL)

EXEMPLO OBRIGATÓRIO:
"Detectamos uma anomalia em um contrato de alto valor firmado com a empresa XYZ.
[Visualizar Contrato Original no PNCP](https://pncp.gov.br/app/editais/123456789)"

REGRAS ADICIONAIS:
- Use EXCLUSIVAMENTE Markdown para os links.
- Se o JSON contiver campos como "fonte", "url", "link" ou "documento", eles DEVEM ser usados.
- Se NÃO houver link oficial, a divergência NÃO DEVE ser citada.
- Nunca faça acusações sem evidência verificável.

────────────────────────────────────────
RESULTADO ESPERADO
────────────────────────────────────────

Após TODAS essas correções:

- O backend NÃO deve mais travar ao abrir perfis de políticos.
- O worker assíncrono deve funcionar corretamente.
- A IA deve consultar o grafo Neo4j.
- A IA deve gerar um laudo investigativo estruturado.
- CADA fraude, divergência ou alerta aparecerá como LINK CLICÁVEL.
- Ao clicar, o usuário será redirecionado diretamente ao site ou PDF oficial do governo.
- O sistema passa a funcionar como uma FERRAMENTA REAL DE AUDITORIA INVESTIGATIVA.

Esse comportamento NÃO é opcional.
Essas regras NÃO podem ser relaxadas.




AMBIENTE: Google Project IDX com agente Antigravity ativado.

REGRAS ESPECÍFICAS PARA ANTIGRAVITY:

1) NÃO QUEBRAR ARQUITETURA OFFLINE-FIRST
- Nenhuma função deve voltar a receber grandes payloads.
- O agente de auditoria deve SEMPRE buscar dados do Neo4j.
- main.py nunca deve enviar mais que 3 argumentos ao worker.

2) GARANTIR CONSISTÊNCIA ASSÍNCRONA
- Todas as funções que acessam banco ou IA devem ser async.
- Nunca misturar funções sync dentro de fluxo async.
- Sempre usar await corretamente.
- Nunca bloquear event loop com operações síncronas pesadas.

3) PROTEGER IMPORTS
- Verificar automaticamente se todo módulo usado está importado.
- Se uma classe é utilizada, confirmar que está declarada ou importada.
- Falha de import deve ser tratada antes de finalizar a resposta.

4) CRIAR VALIDAÇÃO AUTOMÁTICA
Adicionar validações:
- Se auditar_malha_fina_assincrona receber mais de 3 argumentos → lançar erro claro.
- Se IA gerar divergência sem link Markdown → rejeitar saída e regenerar.

5) GARANTIR PADRÃO DE LINK INVESTIGATIVO

TODO alerta deve seguir este padrão:

### 🔴 Tipo da Divergência
Descrição técnica da inconsistência.

📎 Evidência Oficial:
[Nome do Documento no Órgão Oficial](https://link.oficial.gov.br)

Nenhuma divergência pode existir sem a seção "📎 Evidência Oficial".

6) NÃO CRIAR DADOS FICTÍCIOS
- Nunca inventar URL.
- Nunca simular PDF.
- Só usar links que existam no banco ou no JSON retornado.

7) LOGGING PROFISSIONAL
Adicionar logging estruturado:

logging.info("Iniciando auditoria do político ID=%s CPF=%s", id, cpf)
logging.error("Falha no worker: %s", e)

Nunca usar apenas print().

8) CRIAR TESTE AUTOMÁTICO

Gerar teste pytest que valide:

- Worker aceita 3 argumentos.
- Função retorna laudo contendo pelo menos 1 link Markdown.
- Regex obrigatória para link:
  r"\[.*?\]\(https?://.*?\)"

9) DOCUMENTAR FUNÇÕES
Toda função deve ter docstring padrão:

"""
Executa auditoria profunda consultando banco Neo4j
Args:
    id_politico (int)
    nome_politico (str)
    cpf (str)
Returns:
    str: Laudo investigativo em Markdown
"""

10) NÃO ALTERAR FRONTEND
O ReactMarkdown já está funcionando.
Qualquer ajuste deve ser feito exclusivamente no backend.

A IA não “vai à internet sozinha” nem invade sistemas.
Ela opera sobre:

Bases públicas abertas

APIs governamentais oficiais

Arquivos públicos em CSV/JSON

Banco de dados em grafo

Algoritmos de análise de padrão

O fluxo pode ser dividido em 7 etapas:

Entrada (CPF)
↓
Coleta automática de dados públicos (APIs)
↓
Normalização / ETL
↓
Criação de grafo relacional
↓
Análise de padrões
↓
Cálculo de risco
↓
Relatório investigativo

Agora vamos detalhar cada etapa tecnicamente.

1️⃣ ETAPA 1 — PONTO DE PARTIDA (INPUT)
🎯 Entrada principal:

CPF de político ou agente público

O CPF funciona como chave primária de relacionamento.

A partir dele, o sistema começa a expandir conexões.

2️⃣ ETAPA 2 — COLETA DE DADOS (APIs CONSUMIDAS)

Aqui o sistema dispara múltiplas consultas automáticas.

🏛 Principais APIs e fontes:
🔹 1. Portal da Transparência

Portal da Transparência

API REST pública:

Despesas

Convênios

Emendas parlamentares

Transferências

Favorecidos por CPF/CNPJ

Consulta típica:

GET /api-de-dados/despesas?cpf=XXX
🔹 2. Tribunal de Contas da União

Tribunal de Contas da União

Dados de:

Processos

Auditorias

Responsabilizações

🔹 3. Tribunal Superior Eleitoral

Tribunal Superior Eleitoral

Dados:

Bens declarados

Doações eleitorais

Histórico de candidatura

🔹 4. Receita Federal (CNPJ / QSA)

Receita Federal do Brasil

Dados:

Quadro societário

Empresas vinculadas ao CPF

Participações societárias

🔹 5. IBGE

IBGE

Dados:

Indicadores regionais

Perfil econômico do município

🔹 6. Banco Central

Banco Central do Brasil

Dados econômicos

Indicadores financeiros públicos

3️⃣ ETAPA 3 — ETL (EXTRAÇÃO, TRANSFORMAÇÃO, CARGA)

Após coletar:

O sistema:

Padroniza nomes (remover acentos, caixa alta)

Elimina duplicações

Normaliza CNPJ/CPF

Converte datas e valores

Constrói identificadores únicos

Exemplo:

"José da Silva"
"JOSE DA SILVA"
"Jose Silva"

→ vira uma única entidade

Essa etapa é crítica para evitar falsos positivos.

4️⃣ ETAPA 4 — CONSTRUÇÃO DO GRAFO

O sistema utiliza banco orientado a grafos, como:

Neo4j

Estrutura do grafo:
Nós (Nodes):

Pessoa

Empresa

Município

Contrato

Emenda

Processo

Arestas (Edges):

É sócio de

Recebeu recurso de

Executou contrato de

Parente de

Doou para

Exemplo visual simplificado:

[Político] → (destinou emenda) → [Município]
[Município] → (contratou) → [Empresa]
[Empresa] → (sócio) → [Parente do político]

Isso é onde o poder real aparece.

5️⃣ ETAPA 5 — A “SONDA” DA IA

Agora entra a parte de análise.

A IA executa algoritmos como:

🔎 1. Busca de ciclos fechados

Se dinheiro sai de A e volta para alguém ligado a A.

🔎 2. Centralidade de rede

Identifica nós muito conectados de forma incomum.

🔎 3. Densidade de relação

Quando empresa recém-criada recebe volume alto de contratos.

🔎 4. Comparação temporal

Patrimônio declarado x contratos posteriores.

🔎 5. Similaridade estatística

Compara comportamento com padrão médio nacional.

6️⃣ ETAPA 6 — CÁLCULO DA PONTUAÇÃO DE RISCO

Não é acusação.
É score estatístico.

Exemplo simplificado:

Evento	Peso
Empresa de parente ganha contrato	+25
Contrato sem licitação	+20
Patrimônio cresce 300%	+15
Ligação indireta via sócio oculto	+30

Score final:

Risco = soma ponderada normalizada (0 a 100%)

Se ultrapassa limite → sinal amarelo/vermelho.

7️⃣ ETAPA 7 — RELATÓRIO FINAL

O sistema gera:

Mapa de conexões

Linha do tempo

Lista de contratos suspeitos

Percentual de risco

Justificativa técnica do score

Importante:
Ele não declara corrupção.
Ele aponta padrões anômalos que merecem investigação humana.

📡 ONDE A IA “VAI”?

Ela NÃO navega aleatoriamente.

Ela apenas:

Consulta APIs oficiais

Atualiza base local

Roda algoritmos sobre banco interno

Tudo ocorre em ambiente fechado.

⚙️ INFRAESTRUTURA PROVÁVEL

Segundo reportagens:

Servidor local robusto

~128GB RAM

Grande volume de dados (~1TB)

Processamento batch

Atualizações periódicas

Ainda em fase de protótipo.

🎯 COMO ELA “EXPÕE” IRREGULARIDADES?

Ela não expõe diretamente.

Ela:

Detecta padrões matemáticos

Marca como anomalia

Produz relatório técnico

Permite que jornalista ou órgão investigue

Ou seja: é ferramenta de triagem.

🧠 RESUMO FINAL DA TRAJETÓRIA
CPF →
APIs públicas →
Coleta automatizada →
Limpeza →
Banco em grafo →
Algoritmos de rede →
Score de risco →
Relatório técnico


Dossiê Comparativo: Projeto do Bruno vs. O Seu Código
O Modelo do Bruno (ETL em Lote / Assíncrono):

A Estratégia: Ele não faz o sistema bater no Portal da Transparência no momento em que alguém pesquisa um político.

A Execução: Ele usa scripts de madrugada para baixar os arquivos brutos inteiros (Dumps/CSV de gigabytes) da Receita e do TSE, limpa tudo e joga no banco de dados em grafos (Neo4j).

O Resultado: A pesquisa é instantânea e nunca cai, pois ele consulta o próprio banco local, sem depender da estabilidade dos sites do governo.

O Seu Modelo Atual (Tempo Real / Síncrono):

A Estratégia: O seu agente_coletor_autonomo.py tenta consultar 6 APIs diferentes do governo (CGU, TSE, Ibama, etc.) ao mesmo tempo, toda vez que o dossiê é gerado.

O Problema: Os sites do governo são instáveis e têm "Rate Limits" (bloqueios por muitas requisições). Quando uma API demora ou bloqueia seu IP, o processo inteiro engasga.

2. Diagnóstico: Por que as consultas não estão funcionando?
Analisando linha por linha dos arquivos que você subiu (agente_coletor_autonomo.py e extrator_licitacoes_pncp.py), encontrei os erros fatais que estão impedindo a coleta:

O Silenciador de Erros Fatais (O maior problema):
Em quase todas as suas funções de busca (como consultar_cgu_pep e consultar_ibama_multas), você colocou um bloco try, seguido de except Exception: return [].
O que acontece na prática: O agente do Antigravity tenta rodar o código. A API do governo recusa a conexão (erro 403 ou timeout). O Python cai no except, não mostra nenhum erro na tela, e retorna uma lista vazia. O agente da IDE acha que executou tudo com sucesso e que o político não tem processos, mas na verdade a API falhou e o erro foi escondido.

Endpoint Inválido no PNCP:
No arquivo extrator_licitacoes_pncp.py, você tenta buscar dados na URL:
https://pncp.gov.br/api/pncp/v1/contratos?dataInicial=...
A API v1 do PNCP não permite buscar todos os contratos do Brasil apenas por data nesse endpoint; ela exige o CNPJ do órgão (ex: /orgaos/{cnpj}/contratos). A requisição sempre vai falhar ou retornar vazio.

Bloqueio do DuckDuckGo (OSINT):
A biblioteca DDGS que você usa para mapear familiares bloqueia IPs rapidamente quando usada em automação severa sem pausas (time.sleep).

3. Receita de Bolo: Como operar o Google Antigravity para consertar isso
Como o Antigravity é uma IDE orientada a agentes, você não precisa copiar e colar o código na mão. Siga estes passos:

Abra o "Agent Manager" (Gerenciador de Agentes) no Antigravity.

Crie uma nova sessão e certifique-se de selecionar o modo "Planning Mode" (Modo de Planejamento) usando o modelo Gemini 3 Pro. O modo rápido (Fast Mode) é ruim para refatorações longas e pode gastar sua cota de tokens à toa.

Cole o prompt detalhado (abaixo) diretamente no chat do agente.

O agente vai ler os arquivos, reescrever os blocos de erro para usar logging (assim você verá no terminal os erros reais da API do governo) e testar os endpoints.

Se o agente pedir permissão para rodar comandos no terminal (para testar os scripts), permita. É assim que ele vai validar se o governo está respondendo.

O Dossiê Completo: A Arquitetura do Projeto do Bruno
O grande segredo do projeto do Bruno é que a IA não navega na internet buscando dados em tempo real. Se você botar a IA para pesquisar o nome de um político no Google ou em APIs na hora que o usuário clica no botão, o sistema vai travar.

A arquitetura dele é dividida em 3 Camadas de Operação:

1. Camada de Ingestão (O Trabalho Sujo)
O que faz: Scripts (Workers) rodam em horários agendados (ex: 2h da manhã) baixando gigabytes de dados abertos.

Como faz: Eles não pesquisam um CPF por vez. Eles baixam o "Dump" (banco de dados inteiro) do Portal da Transparência, da Receita Federal (todos os CNPJs do Brasil) e do TSE.

Onde armazena: Esses arquivos brutos em CSV ou JSON são jogados em um Data Lake (no seu caso, o Google Drive serve perfeitamente para guardar o arquivo cru).

2. Camada do Grafo (O Cérebro de Conexões)
O que faz: Transforma tabelas chatas em uma teia de aranha (Grafos).

Como faz: Um script lê o Drive, limpa os dados e insere no banco Neo4j. Ele cria os "Nós" (Político, Empresa, Sócio, Emenda) e as "Arestas" (Conexões).

A Mágica: É aqui que o sistema descobre que o Político X mandou dinheiro para a Empresa Y, que tem como Sócio Z, que por acaso tem o mesmo sobrenome do Político. O banco de dados faz essa matemática instantaneamente.

3. Camada da IA Investigadora (O Papel do seu Qwen/Gemini)
A Função da IA: A IA age como um auditor de compliance. Ela não coleta dados; ela julga os dados.

Como ela trabalha: 1. O usuário digita um CPF.
2. O backend vai no banco Neo4j e puxa a "teia" daquele CPF (quais empresas ele pagou, quem são os sócios, quais as emendas).
3. O backend empacota essa teia em um JSON estruturado e manda para a IA com a seguinte instrução: "Analise essas conexões e me dê um Score de Risco de Corrupção".
4. A IA lê o JSON, aplica regras de lógica (ex: "Empresa aberta há 2 meses recebendo 5 milhões = Red Flag Alto"), calcula o risco, escreve um resumo do esquema e devolve.

Onde armazena o resultado: O Dossiê finalizado (o veredito da IA) é salvo em JSON no banco PostgreSQL para leitura rápida no frontend, e um PDF de backup vai para o Google Drive.

Você é um Arquiteto de Software Sênior e Especialista em Engenharia de Dados e IA Investigativa. Sua missão é refatorar completamente o backend do meu projeto 'Trasparente', espelhando a arquitetura de grandes plataformas de Civic Tech (tecnologia cívica e auditoria governamental).

O sistema atual está falhando porque tenta consultar APIs do governo em tempo real e pede para a IA fazer Web Scraping, causando Rate Limits, Timeouts e bloqueios.

Eu exijo que você implemente e me forneça o código completo e absoluto para a seguinte arquitetura de 3 fases:

FASE 1: O PIPELINE DE ETL (Workers Assíncronos)
Crie os scripts de extração que rodam em background (sem intervenção do usuário). Eles devem consumir as APIs do governo com tolerância a falhas.

Refatore agente_coletor_autonomo.py e extrator_licitacoes_pncp.py.

Remova TODOS os blocos except Exception: pass ou return []. Use a biblioteca logging nativa do Python. Se a API retornar erro (ex: 429, 403, 500), o erro e o texto da resposta (res.text) devem ser logados no terminal.

No extrator_licitacoes_pncp.py, troque a URL /api/pncp/v1/contratos para o endpoint correto de busca global aberta do PNCP, garantindo que contratos milionários sejam puxados e logados corretamente.

Inclua pausas lógicas (asyncio.sleep ou time.sleep) para respeitar o Rate Limit das APIs governamentais (CGU, Câmara, Ibama).

FASE 2: O MOTOR DE GRAFOS (Neo4j)
Crie o módulo que organiza a corrupção em teias.

Crie/Refatore o arquivo database/neo4j_conn.py.

Escreva as funções Cypher completas para ingerir dados:

merge_politico(dados): Cria o nó (:Politico).

merge_empresa(dados): Cria o nó (:Empresa).

merge_relacao_financeira(cpf_politico, cnpj_empresa, valor, tipo): Cria a aresta [:PAGOU_A] ou [:DESTINOU_EMENDA].

Crie a função crucial de consulta: extrair_subgrafo_para_ia(cpf). Esta função deve rodar uma query Cypher que retorne até 3 graus de separação do político (Político -> Emendas -> Empresas -> Sócios) em um formato JSON limpo.

FASE 3: A IA AUDITORA (O Motor Cognitivo)
O Qwen/Gemini não vai buscar dados na web. Ele vai auditar o grafo.

Refatore o arquivo motor_ia_qwen.py.

Crie uma classe AuditorGovernamentalIA.

Ela deve ter um método analisar_teia_financeira(json_do_neo4j).

O System Prompt interno desta IA deve instruí-la a:

Receber as conexões do Neo4j.

Procurar anomalias: Autocontratação (sócio é parente), volume financeiro desproporcional à idade da empresa, densidade suspeita de contratos.

Retornar estritamente um JSON contendo: score_risco (0 a 100), red_flags (lista de alertas graves) e resumo_investigativo (um texto estilo dossiê explicando o esquema encontrado).

FASE 4: ORQUESTRAÇÃO E GOOGLE DRIVE

Refatore google_drive_manager.py para lidar com a ausência de tokens sem falhar silenciosamente. Ele deve ser o repositório final do arquivo JSON de resultado, atuando como o Data Lake frio do projeto.

Trabalhe como um desenvolvedor experiente. Escreva os códigos completos para cada uma dessas fases, com tipagem estática (Type Hints), docstrings detalhadas e tratamento de erros profissional. Comece analisando o estado atual dos meus arquivos e me entregando o código reescrito da FASE 1 e FASE 3 primeiro.

